services:
    admin-node:
        build:
            context: ./docker
            dockerfile: ./node/node.Dockerfile
        ports: [ '8081:5173' ]
        command: sh -c "npm install && npm run dev"
        volumes:
            - ./admin:/home/node
